/**
 * Provides additional mapped paths used for testing.
 */
/* eslint-disable */

var System = System || global.System;

System.config(
{
   map:
   {
      'parseconfig': 'test/fixture/parse-config.js'
   }
});